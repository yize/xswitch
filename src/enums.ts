export enum UrlType {
  REG = 'reg',
  STRING = 'string',
}

export enum Enabled {
  YES = 'enabled',
  NO = 'disabled',
}

export enum IconBackgroundColor {
  ON = '#1890ff',
  OFF = '#bfbfbf',
  ERROR = '#f5222d',
}

export enum BadgeText {
  ERROR = 'Error',
  OFF = 'OFF',
  ON = 'ON',
}
