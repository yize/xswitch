export enum UrlType {
  REG = 'reg',
  STRING = 'string'
}

export enum Enabled {
  YES = 'enabled',
  NO = 'disabled'
}

export enum IconBackgroundColor {
  ON = '#3367d6',
  OFF = '#bfbfbf',
  ERROR = '#c53929'
}

export enum BadgeText {
  ERROR = 'Error',
  OFF = 'OFF',
  ON = 'ON'
}
